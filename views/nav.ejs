<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Image Share</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <% if(username) { %>
                    <a class="nav-link" href="/image/up/image">Upload</a>
                <% }%>
            </li>
        </ul>
        <% if(!username) { %>
            <a href="/users/login" class="btn btn-primary my-2 my-sm-0" type="submit">Login</a>
            &nbsp;
            <a href="/users/register" class="btn btn-secondary my-2 my-sm-0" type="submit">Registe</a>
        <% } else { %>
            Hello <%= username %> !
            <a href="/users/logout" class="btn btn-outline-success my-2 my-sm-0" type="submit">Log Out</a>
        <% } %>
        
    </div>
</nav>